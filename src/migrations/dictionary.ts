export enum Tables {
  users = 'users',
  tasks = 'tasks',
}

export enum Columns {
  id = 'id',
  createdAt = 'created_at',
  updatedAt = 'updated_at',

  name = 'name',
  email = 'email',
  password = 'password',
  title = 'title',
}
