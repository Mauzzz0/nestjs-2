const path = require('path');
const dotenv = require('dotenv');

dotenv.config();

module.exports = {
  'env': 'any',
  'config': path.resolve('sequelize.config.js'),
  'migrations-path': path.resolve('dist', 'migrations', 'migrations'),
};
